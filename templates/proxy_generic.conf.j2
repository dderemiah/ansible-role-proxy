<VirtualHost *:443>
    ServerName {{ ansible_facts['fqdn'] }}
    ServerAlias {{ ansible_facts['hostname'] }}

    SSLEngine On
    SSLCertificateFile /etc/apache2/sslcerts/{{ ansible_facts['hostname'] }}.crt
    #SSLCertificateChainFile /etc/apache2/sslcerts/chain.pem
    SSLCertificateKeyFile /etc/apache2/sslcerts/{{ ansible_facts['hostname'] }}.key
 
    ProxyRequests     Off
    ProxyPreserveHost On
    AllowEncodedSlashes NoDecode
    <Proxy *>
        Order deny,allow
        Allow from all
    </Proxy>

    ProxyPass         /  http://localhost:{{ proxy_port }}/ nocanon
    ProxyPassReverse  /  http://localhost:{{ proxy_port }}/
    RequestHeader set X-Forwarded-Proto "https"
    RequestHeader set X-Forwarded-Port "443"
    SSLSessionTickets off
    SSLProtocol -all +TLSv1.2 +TLSv1.3
    SSLHonorCipherOrder on
    SSLCipherSuite EECDH:EDH:!NULL:!SSLv2:!RC4:!aNULL:!3DES:!IDEA

    Header always set Strict-Transport-Security 'max-age=600; includeSubDomains; preload'
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-XSS-Protection "1; mode=block"
</VirtualHost>

<VirtualHost *:80>
    ServerName  {{ ansible_facts['fqdn'] }}
    ServerAlias {{ ansible_facts['hostname'] }}
    Redirect permanent / https://{{ ansible_facts['fqdn'] }}/
</VirtualHost>
