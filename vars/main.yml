#SPDX-License-Identifier: MIT-0
---
# vars file for ansible-role-proxy
apache_mods_enabled:
  - rewrite
  - ssl
  - proxy_http
  - http2
  - headers
  - proxy
  - proxy_wstunnel
apache_vhosts_filename: "{{ ansible_facts['hostname'] }}.conf"
apache_remove_default_vhost: true